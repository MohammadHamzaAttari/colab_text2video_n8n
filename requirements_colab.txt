# ── Web server ────────────────────────────────────────────────
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
python-multipart==0.0.20

# ── ML core — let Colab's pre-installed torch stay ────────────
# Do NOT pin torch/torchvision — Colab ships compatible versions
accelerate>=0.26.0
diffusers>=0.32.0
transformers>=4.46.0
safetensors>=0.4.5

# ── Video export ──────────────────────────────────────────────
imageio>=2.36.0
imageio-ffmpeg>=0.5.1

# ── HuggingFace ───────────────────────────────────────────────
huggingface_hub>=0.27.0
sentencepiece>=0.2.0
protobuf>=4.25.0

# ── Quantisation (8-bit text encoder — saves ~4 GB VRAM) ─────
bitsandbytes>=0.43.0

# ── Colab utilities ───────────────────────────────────────────
nest-asyncio>=1.6.0
pyngrok>=7.0.0
requests>=2.32.0

# ── Attention optimisation (optional, speeds up inference) ────
# xformers must match torch version — install separately if needed:
# pip install xformers